using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HeadRay : MonoBehaviour
{
    Camera cameraTransf;
    // Use this for initialization
    void Start()
    {
        cameraTransf = transform.GetComponent<Camera>();
    }

    // Update is called once per frame
    void Update()
    {
        //Ray ray = cameraTransf.
        //bool isCollider = Physics.Raycast()
    }
}
